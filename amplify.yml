version: 1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple --allow-destructive-graphql-schema-updates
frontend:
  phases:
    preBuild:
      commands:
        #- npm ci
        - npm install --verbose
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
