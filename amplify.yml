version: 1
backend:
  phases:
    build:
      commands:
        - npm uninstall @aws-amplify/cli
        - npm install -g @aws-amplify/cli@7.6.14    
        - amplifyPush --simple --allow-destructive-graphql-schema-updates
frontend:
  phases:
    preBuild:
      commands:
        #- npm ci
        - npm install --verbose
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
